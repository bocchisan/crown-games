# conditional-funding — Спека

Краудфандинг-сбор на форме `two-outcome`. Получатель открывает сбор (`goal` — дисплей, ничего не гейтит,
+ `duration`); доноры скидываются — каждый **одной транзакцией** в свой эскроу, **без подписи канистре**
(сама транзакция — участие). Получатель жмёт «готово» (`ready`) → голосование держателей репутации.
Подтвердили → **всем** эскроу через сплиттер получателю + репутация каждому донору; иначе **всем**
возврат. **Тишина = выплата получателю** — возврат требует кворумного «против». Область = сбор, `B=N`.

Общие инварианты — `crown-spec/docs/games-harness.md`; здесь — вся специфика сбора, дословно.

## Область, идентификатор, резолвер, членство

- **Область = сбор**, ключ `scope_id = collection_id` — **свободный** id (не адрес эскроу):
  ```
  collection_id = sha256( "crown:conditional-funding"
                        ‖ u8(len(canister_id)) ‖ canister_id
                        ‖ recipient(32) ‖ u64le(recipient_nonce)
                        ‖ u64le(duration) ‖ u64le(voting_period)
                        ‖ u16le(approval_threshold) ‖ u128le(quorum_weight) )
  ```
  Конфиг/тайминги — в прообразе (снимок правил закоммичен создателем; при материализации канистра
  пересчитывает `collection_id` из предъявленных полей и сверяет). `collection_id` **не** коммитит
  `gross`/`deadline` (много эскроу одной области). `recipient_nonce` лишь разводит `id`.
- **Резолвер** = `key([collection_id])` (локальная деривация, `get_resolver` — `query`).
- **Членство эскроу — полем `resolver`, не `nonce`** (реестра нет): у каждого вклада
  `salt = crown_salt::two_outcome(donor, recipient, gross, deadline, resolver, fee_bps, fee_wallet, nonce)`,
  где `recipient`/`resolver` — из записи сбора, `fee_bps`/`fee_wallet` — из конфига, не из аргументов;
  адрес = `crown_derive::solana_pda_address(factory, [b"escrow", salt]).0`. `deadline u64→i64`, вне
  диапазона → `DeadlineOverflow`. Иная комиссия/резолвер → другой адрес, и материализовать таким
  вкладом сбор нельзя. (Усечённый хеш в `nonce` вскрываем перебором ~2^64/N — поэтому привязка
  только `resolver`.)
  - **Членство — это `resolver` и больше ничего, и `recipient` сюда не входит.** Формулировка «чужой
    получатель невыводим, в сбор не входит» стояла здесь и была неверна: эскроу с резолвером сбора и
    **любым** `recipient` — полноправный член области, его открывает та же подпись вердикта, и на
    `settle` деньги уходят тому получателю, что вшит в его соль. Канистра этого не видит и видеть не
    может (она видит один вклад — материализовавший). Значит правильный `recipient` в соли — **проверка
    на клиенте донора**, ровно как и `deadline` (§Тайминги), и оба берутся из `get_collection`.
    Комиссию, в отличие от получателя, страхует вторая линия: `fee_bps`/`fee_wallet` входят в
    подписываемое вердикт-сообщение, поэтому вклад с чужим прейскурантом подписи не имеет вовсе
    (§Форматы, харнесс §9). У получателя такой второй линии нет — он не в сообщении; конечный фолбэк
    один и тот же, ончейн-`refund()` по дедлайну.

## Состояния (`logic/`)

```
Funding → Voting{started_at} → Decided{outcome} ;  Decided — поглощающее
outcome ∈ { Settle, Refund }
```
Рождение всегда в `Funding`, `votes` пуст. Действия: `Ready`, `RecipientCancel`, `Vote(v)`, `Tick`.
(`Refund` здесь — это `outcome=1` формы `two-outcome`, которую фабрика зовёт `cancel`; байт и вердикт-сообщение общие, `games-harness §5`.)

## Таблица переходов (полная, `logic/`)

Время применяется **до** действия (лениво); опоздавший вызов действию не помогает; провалившееся
действие сохраняет уже наступивший временной переход. Границы строго `>=` (момент границы — следующей фазе).

| Состояние (условие) | `ready` | `recipient_cancel` | `vote(v)` | время |
|---|---|---|---|---|
| `Funding`, `now < created_at+duration` | → `Voting{now}` | → `Decided{Refund}` | `InvalidTransition` | — |
| `Funding`, `now ≥ created_at+duration` | → `Decided{Refund}`, затем `InvalidTransition` | то же | то же | → `Decided{Refund}` |
| `Voting`, `now < started_at+voting_period` | `InvalidTransition` | `InvalidTransition` | `w<MIN_VOTE_WEIGHT`→`WeightBelowThreshold`; дубль→`DuplicateVoter`; иначе запись | — |
| `Voting`, `now ≥ started_at+voting_period` | тэлли→`Decided`, затем `InvalidTransition` | то же | то же | → `Decided{verdict(votes)}` |
| `Decided` | `InvalidTransition` | `InvalidTransition` | `InvalidTransition` | — |

`ready`/`recipient_cancel` на неоматериализованном сборе (ни одного эскроу) → `unknown collection`.
`checked_add` для `created_at+duration` и `started_at+voting_period`; переполнение → `Overflow`
(состояние не двигается). Ошибки шага: `InvalidTransition · WeightBelowThreshold · DuplicateVoter · Overflow`.

## Правило вердикта (дословно) — с кворумом

```
yes = Σвес(done);  no = Σвес(not_done)      // checked_add; переполнение → refund
turnout = yes + no                           // checked_add; переполнение → refund
turnout < quorum_weight                       → settle
share = yes · 10000;  bar = approval_threshold · turnout   // checked_mul; переполнение → refund
refund ⇔ share < bar    (строго <)
иначе                                         → settle
```
- **Кворум** `quorum_weight` — абсолютный вес в minor units, считается по **обеим** сторонам (`yes+no`),
  не доля собранного (канистра слепа). **Порог одобрения** `approval_threshold` — десятитысячные,
  нестрогое `≥`. `5000` = половина, и ничья её берёт.
- Ничья, недобор кворума, пустое голосование → **settle** (сбор всегда финализируется).
  `settle`→`outcome=0`, `refund`→`outcome=1`.
- **Умолчание — в пользу получателя.** Молчание голосующих оплачивает сбор; чтобы деньги вернулись
  вкладчикам, голосование обязано **состояться** (набрать кворум) **и** выйти против выплаты. Кворум,
  таким образом, больше не охраняет вкладчика — он лишь решает, спрашивают ли долю одобрения вообще.
  Недобравшее кворум «против» выплату не останавливает.
- **Переполнение подсчёта — единственное исключение: → refund.** Это отказ подсчёта, а не вердикт
  голосующих; settle на нём позволил бы подобранными весами вынудить выплату.

## Тайминги

- `created_at` — **слот рождения того вклада, которым сбор материализовали** (из пруфа рождения;
  слот→время линейно по пиннутому якорю: `created_at = genesis_unix + (slot − genesis_slot)·slot_ms/1000`,
  где `slot_ms = 1000/SLOTS_PER_SECOND`; `slot_ms`/`genesis_slot`/`genesis_unix` — поля `config/`-профиля,
  изолированы в `config::slot_to_created_at`, checked; devnet-якорь **замерен** на F5 (`getBlockTime`
  финализованного слота), mainnet-якорь **тоже замерен** (2026-07-31, `P7.22`), фриз-гейт
  `genesis_unix≠0` остаётся), фиксируется при материализации.
  **Безопасную сторону держит наклон, а не сеть.** Прежняя запись здесь — «сеть идёт чуть быстрее,
  поэтому `created_at` уползает вперёд» — верна для devnet и на mainnet **неверна**: замер даёт
  416–422 мс на слот, то есть сеть медленнее номинальных 400, и с наклоном 400 `created_at` выходил бы
  **раньше** настоящего рождения — окно уже заказанного, а требуемый `deadline` мягче, вплоть до
  съеденного 72-часового margin (разбор — `config/mainnet.toml`). Поэтому `slot_ms` берётся заведомо
  **выше** измеренного (`430 > 421.9`): тогда `created_at` позже реального, окно шире, требуемый
  `deadline` строже — ошибка лежит в безопасную сторону. Якорь и наклон стареют вместе и
  перемеряются одним замером; дрейф проверяет F5-драйвер до того, как двинутся деньги. Окно `Funding`:
  `[created_at, created_at + duration]`. `ready`/`recipient_cancel` валидны при `now < created_at+duration`.
  - **«Первый вклад» — это ожидание честного потока, а не проверка.** Канистра слепа и реестра не
    держит: эскроу-поля приходят неподписанными экстрами, и материализовать сбор можно пруфом
    **любого** его вклада, а «первый» она отличить не может — членство есть деривация резолвера.
    Красть этим нечего (сдвиг окна вперёд стоит получателю рано протухших чужих вкладов, деньги
    которых уходят обратно вкладчикам ончейн-`refund()`), но **гарантией это называть нельзя** —
    отсюда следующий пункт.
- `started_at` — канистерное время нажатия `ready`. Окно `Voting`: `[started_at, started_at+voting_period]`.
- `goal` в код не входит; оверфандинг свободен; выплата = Σ собранного − комиссия (сумму считает сервер
  из цепи, канистра — нет).
- `deadline` каждого эскроу ≥ `created_at + duration + voting_period + DEADLINE_MARGIN` (72 ч) — обеспечивает
  UI донора (регистрации нет), иначе `refund()` обгонит `claim(settle)`. **Канистра проверяет это
  правило только для того вклада, которым сбор материализован** (`validate_registration`); вклады со
  второго по N-й в неё не приходят вовсе, значит арифметика на клиенте — и до `P8` считать её было
  не из чего: `collection_id` это хеш, из него не достать ни `created_at`, ни получателя. Поэтому
  окно **публикуется**: `get_collection` отдаёт `created_at`/`duration`/`voting_period`/`recipient`
  (§Методы). Клиент, занизивший `deadline`, делает свой эскроу возвратным **до** вердикта. С умолчанием в пользу
  получателя это окно несёт **больше** веса, чем раньше: молчаливый сбор теперь заканчивается
  `settle`, и получателю надо успеть заклеймить за эти 72 ч. Ончейн-`refund()` остаётся
  permissionless после `deadline` и отдаёт 100% вкладчикам (`crown-factory/docs/spec.md`, форма
  обязательна) — то есть перевёрнут **вердикт**, а не конечный фолбэк: если получатель не пришёл или
  канистра лежит дольше окна, деньги всё равно вернутся.

## Константы и профиль

| Константа | Значение |
|---|---|
| `LOGIC_VERSION` | `4` (умолчание вердикта перевёрнуто в пользу получателя) |
| `MIN_DURATION` | `60` (1 мин), граница включительна |
| `MAX_DURATION` | `7_776_000` (90 сут) |
| `MIN_VOTE_WEIGHT` | `100_000` |
| `MIN_APPROVAL_THRESHOLD` | `5_000` (нижняя граница включительно) |
| `APPROVAL_THRESHOLD_SCALE` | `10_000` (знаменатель и верхняя граница исключительно) |
| `DEADLINE_MARGIN` | `72 ч` (забота UI, не в `logic/`) |
| `V_MAX` | `250` | кэп голосов на сбор (инв. #7) — **выведен из маржи**, а не выбран: голос замерен на `P8` дороже модели (27.6e6 циклов против 15e6), и при `500` сбор у флора, добравший голоса до кэпа, уходил в минус. Закон в `crown-games-common::field` |
| `ROOT_CACHE` | `4` — сколько аутентифицированных корней индекса остаются допустимыми одновременно (§Методы, `push_root`) |
| `MAX_ARG_BYTES` | `8 KiB` — кэп аргумента ingress; режется первым, до разбора; выведен из ёмкости поколения, не выбран (харнесс §6) |
| `sign_price` / `root_price` | `26.2e9` / `1.0e9` (devnet), `35.8e9` / `1.4e9` (mainnet); оба ≤ релейных — цена оплаченного pull'а подписи и пуша корня |

Создание: `duration ∈ [MIN_DURATION, MAX_DURATION]` (иначе `DurationOutOfRange`);
`approval_threshold ∈ [5000, 10000)` (иначе `ThresholdOutOfRange`).

Профиль (снимок при рождении, входит в прообраз `id`):

| Профиль | `voting_period` | `approval_threshold` | `quorum_weight` |
|---|---|---|---|
| devnet | `120` | `5000` | `150_000` |
| mainnet | `3600` | `5000` | `1_000_000` |

Валидация при деплое: `approval_threshold ∈ [5000,10000)`; `quorum_weight ≥ MIN_VOTE_WEIGHT`;
`fee_bps < 10000`; `domain` непуст; `id` непуст, `ascii_graphic`, без `:` и `\n`; цепи попарно различны
по `(id, domain, factory)`; `factory`/`fee_wallet` base58 → 32 байта. Конфиг: `crown_index,
threshold_key, voting_period, approval_threshold, quorum_weight, min_gross, sign_price, root_price` +
якорь слот→время (`slot_ms, genesis_slot, genesis_unix`, §Тайминги); per-chain `id, factory, domain,
fee_bps, fee_wallet`. Нет: сплиттер, казна, RPC-URL.

**`min_gross` = флор вклада**, проверяется **первым** в `validate_registration` (до диапазона
`duration` и запаса дедлайна): это самая дешёвая проверка, и обречённый пылевой вызов не должен
стоить больше неё. Подпись сбора делится на `N` вкладов, но **худший случай — `N = 1`**, когда её не
на что делить; флор считается по нему и тем самым покрывает все `N`. Значение —
`be($1.8325 при N=1)·(1+MARGIN) = $2.199`, в конфиге `2_200_000` (`cost.md §5`, замер `P7.22`).
До `P7.22` здесь стояло `410_000` — амортизированное число из примера при `N = 10`, и оно
противоречило правилу, записанному строкой выше; на измеренной цене вписи оно вдобавок оказалось ниже
break-even при **любом** `N`. Опустить флор по-прежнему можно — но это субсидия с известной ценой,
а не выбор между двумя одинаково верными числами (таблица — в `config/mainnet.toml`).
Алайнмент `MIN_GROSS ≥` индексного `MIN_GROSS` ($0.20) — шаг cost-gate на `P8`, **не тест**: он
существовал и сравнивал наш флор с литералом-копией чужого, то есть покраснеть при том событии, ради
которого написан, не мог (`P7.13`, разбор — в `canister/src/config.rs`). Сверять надо по `net`:
индексный флор применяется к `Settled.gross`, а туда через форму приходит сумма **после** комиссии.

## Форматы сообщений

**Кошельковая подпись участника** — UTF-8, одно поле на строку, `key: value`, фиксированный порядок:
```
crown:conditional-funding:v1
action: <create|ready|cancel|vote>
chain: <id>
canister: <principal>
collection: <hex(collection_id)>
goal: <dec>          # только create
duration: <dec>      # только create
choice: <done|not_done>   # только vote
```
`DOMAIN = "crown:conditional-funding:v1"` (версионный). Слова заморожены. Проверка: **первая строка ≡
`DOMAIN`** (до подписи — она дешевле), signer 32 байта, Ed25519 `verify_strict`, адрес = pubkey.
Кодирование инъективно (тест).

**Домен именно проверяется, и до `P8` — нет.** Он был записан как первая строка сообщения и в разборе
пропускался: строка не несёт `": "`, а `target_ok` связывал только `canister` и `chain`. Значит
авторизацией служила **любая** подпись над **любым** текстом, где нашлись строки
`action:`/`chain:`/`canister:`/`collection:` — например «подтвердите вход», подсунутое получателю
сбора: из такой подписи собирался работающий `recipient_cancel` (сбор гасится) или `vote`. Ровно
поэтому доменное разделение и существует; проверка — одно сравнение, живёт в общем
`crown-games-common::request::parse` (одна граница на все игры, не копия в каждой).

**Вердикт (в `claim`, побайтово, `games-harness.md §5`):**
`verdict_message = "crown:two-outcome:<cluster>" ‖ program_id(32) ‖ u8(outcome) ‖ u16le(fee_bps) ‖ fee_wallet(32)`,
`outcome`: `settle=0`, `refund=1`. **Одна подпись** сбора `key([collection_id])` над этим сообщением —
переиспользуется всеми N эскроу (у всех `resolver = key([collection_id])`); проверяется on-chain против
поля `resolver`. Это и есть атомарность: один `outcome` на все эскроу сбора, без Merkle-корня/пути.

`fee_bps`/`fee_wallet` в сообщении — из **конфига игры**, и форма сверяет суффикс с собственными полями
эскроу. Здесь это несущее, а не перестраховка: канистра видит **только первый** вклад (он материализует
сбор пруфом рождения), а вклады со второго по N-й в неё не приходят вовсе — их членство есть деривация
резолвера. Пока комиссии в сообщении не было, такой вклад расчётывался общей подписью сбора с
`fee_bps = 0`, и платформа не получала с него ничего (харнесс §9). Теперь подпись сбора его не открывает,
и он возвращается вкладчику по дедлайну.

## Методы (`.did`, фиксирован)

**Update:** `create_collection` · `ready` · `recipient_cancel` · `vote` · `request_signature` ·
**`push_root`**.
**Query:** `get_collection` · `get_resolver` · `get_signature` · `get_sign_price` · `get_logic_version`.
Плюс инфраструктурные (как в tasks): `init(opt InitArgs)` (testnet-оверрайды index/NNS-root, на
mainnet — trap) и `bootstrap()` — одноразовый идемпотентный фетч мастер-Schnorr-ключа
(`schnorr_public_key`) после деплоя (sync `init` не может, таймеры запрещены).

`get_signature(collection)` — свободный `query`: подпись сбора, произведённая один раз. Именно им
амортизация `s/N` становится фактом — второй и последующие эскроу читают байты здесь, а не покупают
подпись заново.

`get_collection(collection)` — свободный `query`, отдаёт `CollectionView`: `state` +
**окно сбора** — `created_at`, `duration`, `voting_period`, `recipient` (base58). Это не удобство, а
единственный источник для правила дедлайна (§Тайминги): его арифметика лежит на клиенте донора,
канистра сверяет её лишь для материализовавшего вклада, а из `collection_id` (хеш) ни окно, ни
получателя не восстановить. Прежде `query` отдавал один вариант состояния, и вкладчику со второго по
N-й оставалось `created_at` **угадывать**; занижение делает эскроу возвратным до вердикта.

**`get_sign_price()` — свободный `query`: во что обходится подпись сейчас.** Отдаёт `max(`забейканный
`SIGN_PRICE`, цена, которую котирует сам IC для нашего ключа`)`, и это же число гейтит оплату в
`request_signature`. Забейканная цена берётся из `config/` на сборке, а настоящую назначает сеть —
пока константа больше, не меняется ничего; в день, когда сетевая цена её перегонит, игра, продолжающая
брать константу, подписывала бы **в убыток**, и единственным симптомом был бы падающий баланс циклов.
Запрос свободный именно затем, чтобы мониторинг видел сближение двух чисел **до** того, как его увидит
первый расчёт: релейный `sign_price` обязан быть не ниже этого. Забрикетить нельзя — сегодня ответ
равен константе побайтово, а если сетевая цена её перегонит, `request_signature` ответит `Underpaid`
вместо подписи в убыток: это громкий простой, чинимый двумя строками конфига (ни игры, ни релеер не
заморожены), а не тихая утечка. Эскроу это не касается — они возвращаются по дедлайну без подписи вовсе.

**`push_root(cert)` — оплаченный пуш корня индекса** (фронтит релеер за `ROOT_PRICE`). Канистра
аутентифицирует сертификат индекса (две BLS-пары: подпись индекса + делегация сабнета) против NNS
root key и кладёт `combined_root` в кеш `ROOTS` (глубина `ROOT_CACHE = 4`). Дальше **всякий** пруф
рождения/репутации — обход хеш-дерева против любого корня из кеша, без пар.

Зачем расщепление: BLS-пары не влезают в бюджет `inspect_message` (200M инструкций) — прямой ingress
`create_collection` с сертификатом отбивался бы `CanisterInstructionLimitExceeded`, то есть **валидная**
материализация не проходит вовсе; а бесплатной на `update` эта проверка была бы реплицируемой парой на
любую анонимную строку байт. Платный разовый пуш делает дорогим ровно один шаг и оставляет дешёвыми
все per-caller пути (`cost.md §6` #2). Оплата принимается **до** пар, на кривом сертификате не
возвращается (`01-standards §Тесты 4`). Кеш из нескольких корней нужен потому, что корень индекса
двигается на каждой вписи: свидетель, собранный против актуального на тот момент корня, обязан
пройти, пока не приедет следующий `push_root`. Повторный пуш того же корня освежает позицию, а не
растит кеш (иначе реплей вытеснил бы все живые корни). Полностью совпадает с эталоном
(`conditional-tasks`) — это инвариант харнесса, а не деталь одной игры.

Query состояния игры **не сертифицированы** (`+witness` снят по всему проекту): доверие в Crown течёт через
threshold-подпись резолвера (проверяется on-chain) и сертифицированные пруфы **индекса** (книга/рождения),
а не через состояние канистры-игры — деньги эскроу клеймит против подписи, не против query.
`request_signature(chain, collection_id)`: если `Decided` — оплаченный pull производит **одну** подпись
сбора (кэш по `collection_id`), возвращает `(outcome, sig)`; иначе `collection not decided`. Порядок
`vote`: подпись → время(`Tick`) → состояние `Voting` → дедуп `(collection_id, voter)` → пруф веса;
валидность — в `inspect_message`.

**Граница (`inspect_message`, харнесс §6):** каждый меняющий состояние вызов доказывает применимость до
раунда — `create_collection` пруфом рождения **против кешированного корня** (обход хеш-дерева, O(log n);
BLS сюда не идёт — она на оплаченном `push_root`) и материализует,
`ready`/`recipient_cancel` — подписант ≡ получатель + состояние, `vote` пруф веса + порог (тоже против
кеша).

**Пруф рождения у `create_collection` обязателен, эхо деривации удалено (`P8`).** Раньше `create` без
свидетеля отвечал `Derived` (`id` + резолвер, ноль записи) и допускался границей — «ноль записи»
читалось как «безвредно». Это неверно: получатель сбора — это `req.pubkey`, то есть отправитель
подписывает **собственный** `create`, и валидный беспруфовый запрос печатается со свежего ключа даром.
Каждая копия исполнялась **реплицированно** (`verify_strict` + SHA-256 + деривация Ed25519-подключа) за
счёт канистры, без границы сверху — ровно тот шаблон флуда, против которого в `admit_action` заведена
проверка состояния (харнесс §6), и единственный путь, перед которым не стояло никакого пруфа.
Потребителя у эха не было: `collection_id` вызывающий держит **до** вызова (это подписанное поле
сообщения — он посчитал его, чтобы подписать), а резолвер отдаёт `get_resolver`, свободный `query`.
Первым режется размер аргумента (`MAX_ARG_BYTES = 8 KiB`).

**`bootstrap` — окно, а не защёлка.** Допускается, только пока мастер-ключ не взят **и** свободно окно
попытки. Мастер-ключ ложится после `await`, поэтому до тех пор проверка
«ключ уже есть» пропускает всех, и каждый анонимный вызывающий получает свой вызов в management
даром. Гейт — метка времени: попытка резервирует 30 с, граница окно читает, апдейт забирает, окно
открывается **само**. Claim-до-`await`, как у подписи, здесь был бы багом: он переживает трапнувший
коллбэк, а этот флаг не на область, а на канистру — залипнув, он оставил бы игру без резолверов
навсегда (харнесс §7).

`request_signature` и `push_root` — relay-fronted (inter-canister), границу не
проходят; прямой ingress на них не декодируется как один `text` и отбивается. Обречённое отбивается
не-реплицируемо; `update` авторитетен.

**Раз границу они не проходят, кэп аргумента у них — свой.** `MAX_ARG_BYTES` живёт в
`inspect_message`, а межканистерный вызов его не исполняет; значит на `push_root`/`request_signature`
тот же кэп пере-утверждается **в теле метода**, первой строкой, до оплаты и до CBOR-разбора — ровно
как релеер пере-утверждает его на своём реплицируемом пути и по той же причине (кэп — кноб стоимости,
харнесс §6). Без этого один вызывающий-канистра отдаёт нам на разбор многомегабайтный блоб.

**Реплей `create` умирает до деривации.** Уже материализованный `collection_id` обречён по одному
только `id`, поэтому `is_materialized` проверяется **до** деривации резолвера и перебора bump'а PDA:
подписанная половина запроса нонса не несёт и реплеится даром, и платить за две деривации на каждой
копии граница не обязана.

## Терминальные исходы: деньги / комиссия / репутация

| Исход | Деньги (на каждый эскроу) | Комиссия | `Settled` | Репутация |
|---|---|---|---|---|
| `settle` (0) | `fee → fee_wallet`; `splitter.donate(recipient, gross − fee)` | `gross·fee_bps/10000` | `donor = escrow` | **донору вклада**, `= gross − fee` |
| `refund` (1) | 100% → донор | нет | нет | нет |
| `refund()` (ончейн, после `deadline`, кто угодно) | 100% → донор | нет | нет | нет |

Получатель получает `Σ gross − Σ комиссий`, не больше; приза нет. Клеймы независимы (батч `K` на транзакцию).

## Специфика харденинга и крайние случаи

- **Ленивое создание:** запись появляется на **профинансированном вкладе, предъявленном пруфом
  рождения** — он же и единственный вход в `create_collection`; там же фиксируется `created_at` (слот).
  В честном потоке это первый вклад, но канистра слепа и «первый» не проверяет — какой вклад
  предъявили, тот и якорь (§Тайминги). Снимок профиля — в прообразе. `ready`/`recipient_cancel` **не** материализуют (самоподписаны, бесплатны). Беспруфового
  `create` больше нет: чистая деривация ничего не пишет, но реплицированно **исполняется**, а
  потребителя у неё не было (см. §Граница).
- **Без таймера:** окна и тэлли — чистые функции; safety — ончейн-`refund()`.
- Отмена — **только из `Funding`** (после «готово» двери нет); досрочного выхода донора нет.
- Оверфандинг свободен; `goal` — дисплей.
- `donor == recipient` и самоголосование не запрещены — и **«сдерживаются публичностью» здесь надо
  читать буквально: не ценой.** Вес голоса — репутация голосующего у получателя, а она покупается
  прямым донатом себе на второй кошелёк (сплиттер режет лишь совпадение ATA, `00 §10.1`), то есть
  стоит оборота суммы плюс `INGEST_PRICE` ≈ $0.023 за впись. С перевёрнутым умолчанием покупать
  получателю больше **нечего**: тишина и так `settle`, и чужие вклады уходят ему без единого голоса.
  Купленный вес стал ценой **отказа** — чтобы деньги вернулись, вкладчикам надо набрать
  `quorum_weight = $1.00` и выйти против выплаты, причём ничью берёт получатель (порог нестрогий),
  так что тем же дешёвым весом на стороне `yes` он гасит их «против». Механика тут ни при чём:
  правило вердикта такое и записано, а конечный фолбэк не тронут — ончейн-`refund()` остаётся у
  вкладчика после `deadline`.

  **Голосование сбора социально, и это записанное решение, а не пропуск** (`00 §10.1`, принято
  2026-08-02). `quorum_weight = $1.00` и `approval_threshold = 5000` не претендуют стоять против цены
  покупки веса и не будут подняты до неё: вес покупается за собственный оборот плюс пошлину, потолка
  у такого числа нет, а видимость защиты хуже её отсутствия. Самодонат и самоголосование допустимы
  by design. **Вклады охраняет не голосование, а форма:** ончейн-`refund()` permissionless после
  `deadline`, 100% вкладчику, работает при лежащей канистре и невзятом резолвере — вот граница, ниже
  которой сбор не опускается ни при каком исходе голосования. Голосование лишь распределяет исход
  раньше дедлайна. Витрина обязана говорить это теми же словами (`08-deferred.md`, `crown-app`).
  После запуска числа менять дороже — `quorum_weight`/`approval_threshold`/`voting_period` вшиты
  в прообраз `collection_id`, а игра заменяется, а не апгрейдится.
- Вес голоса — репутация у получателя на момент голоса (снапшота нет), чейн-локально; докупка во время
  голосования ретроактивно не учитывается.
- «Стоящий вердикт»: эскроу, деривуемый к сбору, получает его исход, когда бы ни родился. Частичное
  исполнение: неклеймленный эскроу жив с целым `gross`, репутации не даёт, выходит `refund()` по `deadline`.
- «Готово» — заявление, не пруф; сдачу подтверждают голосующие.
- **Репутация за вклад держится на вписи его рождения, и `B=N` — это тот самый случай, ради которого
  индекс гейтит порядок.** Атрибуция читает `escrow → donor` из уже свёрнутых рождений (`00 §4`).
  У задания (`B=1`) порядок держится сам: подписи резолвера не существует, пока пруф рождения не
  предъявлен. **У сбора это не так** — канистра видит только первый вклад, вклады со второго по `N`-й
  открываются той же подписью, никакого пруфа не предъявив, а `claim` permissionless: как только сбор
  вынес `settle`, кто угодно одной транзакцией расчитывает **чужой** эскроу. Раньше это означало, что
  расчёт, поданный в индекс раньше рождения, навсегда уводил репутацию вкладчика на адрес эскроу.
  Теперь такой сеттлмент **не сворачивается вовсе** (`UnknownBirth`, ноль записей, сигнатура свободна —
  `crown-indexer/src/state.rs::attributable`, `00 §5`): ошибка стала транзиентом, который чинится
  вписью рождения и повторной подачей той же сигнатуры.

  Отсюда — обязанность витрины, уже операционная, а не корректностная: **впись рождения вклада
  покупается в момент подтверждения вклада**, а не когда дело дойдёт до `claim`. Иначе первый же
  `claim` упрётся в `UnknownBirth`, и вкладчик будет ждать репутации, пока кто-нибудь не догадается
  свернуть его рождение (`07-build-plan.md §Контракт подающего впись`, п. 4).
- **Комиссия вклада — не только вопрос деривации.** Канистра видит один вклад, первый; остальные
  входят в сбор полем `resolver` и в неё не приходят. Поэтому «иная комиссия → другой адрес, в сбор не
  входит» гейтит материализацию, а деньги гейтит суффикс `u16le(fee_bps) ‖ fee_wallet` в
  вердикт-сообщении (§Форматы, харнесс §9): подпись сбора не открывает вклад, рождённый с чужим
  прейскурантом. Такой вклад не заперт — он возвращается вкладчику ончейн-`refund()` по дедлайну.

## DoD / тесты (`crown-spec/docs/01-standards.md §Тесты 8,9,12`)

`logic/` zero-dep, property-тесты вердикта (кворум по обеим сторонам, нестрогий порог, ничья/недобор/пусто
→ settle, кворумное «против» → refund, переполнение → refund); границы окна точные; `Overflow` не двигает
состояние. Слепа, без
реестра. Ленивое создание: `ready`/`cancel` без пруфа рождения — ноль записи, а `create` без пруфа
не допускается границей вовсе. Атомарность: одна
подпись сбора на все N эскроу, один `outcome`. Граница: обречённый вызов (плохой пруф рождения/веса, не
тот подписант) отбит в `inspect_message`, до реплицируемого `update`; голос — дедуп
`(collection_id, voter)`. `collection_id`-кодировка пиннута вектором. E2e через реальную сеть.
