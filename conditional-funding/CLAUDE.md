# conditional-funding — CLAUDE.md

Игра класса B на форме `two-outcome`. Обобщение `conditional-tasks` с одного эскроу на **набор** (сбор).

Контракт игр — `crown-spec/docs/games-harness.md`. Строгость/глоссарий — `crown-spec/CLAUDE.md`.
Спека — `docs/spec.md`; план — `docs/build-plan.md`.

Специфика:
- **Область = сбор, N вкладов (`B=N`).** Подпись делится на вкладчиков → эффективный минимум ~$0.26–0.41.
- **All-or-nothing:** подтвердили → всем через сплиттер получателю; иначе всем возврат. Тишина = возврат.
- **С кворумом** (в отличие от tasks/auction): вердикт по `quorum_weight` + `approval_threshold`.
- Канистра **слепа и без реестра**: членство эскроу — деривация, не список.
