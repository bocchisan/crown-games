# conditional-funding — devnet-профиль.
crown_index        = "aaaaa-aa"           # principal crown-indexer
threshold_key      = "dfx_test_key"
voting_period      = 120                   # devnet; в прообразе collection_id
approval_threshold = 5000                  # строгое большинство
quorum_weight      = 150_000
sign_price         = 26_200_000_000        # цена подписи сбора (harness §5; ≤ релейного SIGN_PRICE), амортизируется на N вкладов
slot_ms            = 400                    # 1000/SLOTS_PER_SECOND (SLOTS_PER_SECOND=2.5)
genesis_slot       = 0                      # якорь slot→unix (placeholder до F5 devnet)
genesis_unix       = 0                      # якорь slot→unix (placeholder до F5 devnet)

[[chain]]
id         = "devnet"
factory    = "PLACEHOLDER_factory_two_outcome"
domain     = "crown:two-outcome:devnet"
fee_bps    = 300
fee_wallet = "PLACEHOLDER_fee_wallet"
